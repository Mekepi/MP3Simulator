{
    "name": "node-test",
    "main": "index.js",
    "scripts": {
        "dev": "nodemon --watch src/ --exec ts-node --transpile-only --files ./src/index.ts --ignore ./tests",
        "test": "jest --coverage=false",
        "test:coverage": "jest",
        "lint": "eslint src/ tests/",
        "rm-dist": "rm -r ./dist",
        "build": "tsc"
    },
    "devDependencies": {
        "ts-node": "^10.9.1",
        "typescript": "^4.9.5"
    },
    "dependencies": {
        "dotenv": "^16.0.3",
        "get-mp3-duration": "^1.0.0"
    }
}
